// Operador ternário - 08

// (expressão) ? (se verdadeiro) : (se falso)

const number = 5;
console.log(number % 2 === 0) ? "Par" : "Ímpar";

const age = 22;
console.log(age >= 18) ? "Adulto" : "Menor de idade";
